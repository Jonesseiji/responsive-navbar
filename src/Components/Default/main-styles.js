import styled from "styled-components";
import { COLORS } from "../../constants/colors";

export const Main = styled.div`
  display: flex;
  width: 100vw;
  height: 200vh;
  background: ${COLORS.DEFAULT_DARK};
`;
